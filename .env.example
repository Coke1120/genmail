# Optional local settings loaded by npm start and npm run backup.
# Shell environment variables take precedence. Keep provider keys in the app's Settings.
PORT=3001
DATA_DIR=./data

# By default APP_URL uses http://localhost:<PORT>. No remote hosts are supported.
# APP_URL=http://localhost:3001

# Enables a startup check that the frontend production build exists.
# NODE_ENV=production
